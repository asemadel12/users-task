[project]
name = "make-work-flow-backend"
version = "0.1.0"
description = "Backend API for the MAKE WORK FLOW technical task"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.0,<1.0.0",
    "psycopg[binary]>=3.2.0,<4.0.0",
    "sqlalchemy>=2.0.0,<3.0.0",
    "uvicorn>=0.35.0,<1.0.0",
]
